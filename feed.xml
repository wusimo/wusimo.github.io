<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://wusimo.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://wusimo.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-05-17T06:44:07+00:00</updated><id>https://wusimo.github.io/feed.xml</id><title type="html">blank</title><subtitle>Notes on machine learning, robotics, and other things worth writing down. </subtitle><entry><title type="html">AR-Retrofit and ReSkip: Attention Residuals on a Frozen Backbone</title><link href="https://wusimo.github.io/blog/2026/ar-retrofit-and-reskip/" rel="alternate" type="text/html" title="AR-Retrofit and ReSkip: Attention Residuals on a Frozen Backbone"/><published>2026-05-17T04:00:00+00:00</published><updated>2026-05-17T04:00:00+00:00</updated><id>https://wusimo.github.io/blog/2026/ar-retrofit-and-reskip</id><content type="html" xml:base="https://wusimo.github.io/blog/2026/ar-retrofit-and-reskip/"><![CDATA[<p><em>Part 3 of a series on attention residuals and AR-Retrofit. <a href="/blog/2026/attention-and-residuals/">Part 1</a> set up the residual stream; <a href="/blog/2026/attention-residuals/">Part 2</a> replaced the fixed <code class="language-plaintext highlighter-rouge">+</code> with learned attention over depth.</em></p> <h2 id="the-problem-this-post-is-about">The problem this post is about</h2> <p>Attention Residuals are nice. They give us input-dependent depth combination and a pre-execution routing signal. But the recipe in Part 2 assumes you are training from scratch.</p> <p>The models we actually want to deploy — Qwen3, Llama, the VLM backbones built on top of them — were trained with the fixed scalar-1 <code class="language-plaintext highlighter-rouge">+</code>. Their weights are co-adapted to that recipe: block \(l\) expects its input to be exactly \(x_{l-1}\), the sum of everything written into the stream up through block \(l-1\). Swap the <code class="language-plaintext highlighter-rouge">+</code> for a softmax over \(\{h_0, \ldots, h_{l-1}\}\) and the block sees an input from a distribution it has never been trained on. Performance collapses.</p> <p>A full retrain is the obvious answer and the one we cannot afford. So the question is:</p> <blockquote> <p>Can we keep the pretrained backbone frozen, add attention residuals as a side-channel, and recover the benefits of AttnRes for under half a percent of new parameters?</p> </blockquote> <p>The answer is yes, and the design that makes it work is <strong>AR-Retrofit</strong>. The inference-time payoff — using the learned routing weights to skip blocks — is <strong>ReSkip</strong>. This post is the recipe and the things that almost worked but did not.</p> <h2 id="what-we-need-to-preserve">What we need to preserve</h2> <p>Two things are non-negotiable when retrofitting onto a frozen backbone:</p> <ol> <li><strong>Identity at initialization.</strong> At training step zero, the modified model must produce exactly the same outputs as the pretrained model, up to numerical noise. Otherwise we start training from a worse-than-pretrained point and may never catch up.</li> <li><strong>Input manifold preservation.</strong> Each frozen block \(\mathrm{Block}_n\) was trained to expect inputs from a specific distribution. The retrofit must not push that distribution too far. Direct replacement of \(x_n\) with a softmax-weighted \(r_n = \sum_i \alpha_{i \to n} h_i\) does push it too far — even when \(\alpha\) is initialized to peak on \(n-1\), the soft mass on other \(i\) shifts the input enough that frozen blocks degrade.</li> </ol> <p>Both constraints point in the same direction: the retrofit cannot be a <em>replacement</em> of the residual, it has to be a <em>correction</em> on top of it, gated to start at zero and ramp up.</p> <h2 id="the-ar-retrofit-recipe">The AR-Retrofit recipe</h2> <p>Here is the full recipe between two frozen blocks. Let \(h_{n-1}\) be the output of the previous block (what the standard residual stream would have carried into block \(n\)).</p> \[\begin{aligned} \tilde k_i &amp;= \mathrm{RMSNorm}(h_i) + b_i \\ \alpha_{i \to n} &amp;= \mathrm{softmax}_i\!\left(\frac{w_n^\top \tilde k_i}{\sqrt d}\right), \quad i = 0, \ldots, n-1 \\ r_n &amp;= \sum_{i=0}^{n-1} \alpha_{i \to n}\, h_i \\ x_n &amp;= h_{n-1} \;+\; \gamma_n \cdot A_n\!\left(r_n - h_{n-1}\right) \\ h_n &amp;= \mathrm{Block}_n(x_n) \quad (\text{frozen}) \end{aligned}\] <p>with the <strong>bottleneck adapter</strong> \(A_n(u) = W_{\text{up}}\, \sigma(W_{\text{down}}\, u)\) at rank \(r = 256\), and <strong>gate</strong> \(\gamma_n\) a learnable scalar following a curriculum from \(0\) at the start of training to \(1\) at roughly 30% of the way through.</p> <p>Five design choices are doing real work here. Each was the wrong answer at least once on the way to this one.</p> <h3 id="1-the-base-path-is-still-the-standard-residual">1. The base path is still the standard residual</h3> <p>Notice that the <em>first</em> term of \(x_n\) is just \(h_{n-1}\). If you delete the entire AR-Retrofit machinery you get back the original model exactly. The retrofit only ever adds a correction; it never replaces the base.</p> <p>This is what “identity at initialization” buys you. At \(\gamma_n = 0\) the correction term vanishes, \(x_n = h_{n-1}\), and the model is the frozen backbone running unchanged. We can verify this empirically — at \(\gamma = 0\) the retrofit reproduces base model outputs to bf16 noise.</p> <h3 id="2-the-correction-is-on-r_n---h_n-1-not-r_n">2. The correction is on \((r_n - h_{n-1})\), not \(r_n\)</h3> <p>The thing fed into the adapter is the <em>difference</em> between the routed state and the standard residual. If \(\alpha\) collapses to one-hot at \(i = n-1\), then \(r_n = h_{n-1}\), the difference is zero, and the adapter has nothing to do. We have not perturbed the input manifold at all.</p> <p>This is what lets the router learn freely. A router that is forced to do something useful even when the standard residual is fine ends up corrupting easy cases. By subtracting off the standard residual we make the correction <em>optional</em> — the router only matters when the routed combination genuinely differs from the previous-state baseline.</p> <h3 id="3-gamma-curriculum-0-to-1-over-the-first-30">3. \(\gamma\) curriculum, \(0 \to 1\) over the first 30%</h3> <p>A learnable gate alone is not enough. If \(\gamma\) is initialized small but free, gradient signal pushes it up before the router and adapter have learned anything coherent — and a half-trained router driving a half-trained adapter at non-trivial \(\gamma\) pushes the frozen block off its input manifold.</p> <p>The fix is to schedule \(\gamma\) explicitly. Start at exactly zero. Hold for a few percent of training so the router and adapter can train on a clean gradient signal (gradients still flow even at \(\gamma = 0\) because the adapter’s up-projection is initialized nonzero). Then ramp linearly to one over the next 25–30% of training. By the time the gate is fully open, the modules have learned a correction worth applying.</p> <h3 id="4-the-adapter-is-low-rank">4. The adapter is low rank</h3> <p>At rank \(r \approx 256\) on \(d\) in the low thousands, the adapter dominates the new-parameter cost — but the cost is small. The whole side-channel ends up well under half a percent of a multi-billion-parameter backbone.</p> <p>There is no obvious reason higher rank would help. The router does the interesting work — choosing <em>which</em> prior states to combine. The adapter only has to express a small correction in the direction the router chose; a low-rank bottleneck is plenty.</p> <h3 id="5-block-level-granularity">5. Block-level granularity</h3> <p>Do not apply AR-Retrofit per layer. Group \(L \approx 4\) contiguous transformer layers into a <em>block</em> and apply one retrofit per block. This matches Kimi’s from-scratch finding for AttnRes (\(S \in \{2, 4, 8\}\) all work reasonably; finer than that and the router has nothing useful to say between adjacent layers).</p> <p>Block granularity also keeps the router’s pre-execution signal interpretable: “block \(n\) matters” is a useful unit. “Layer 37 matters” rarely is.</p> <p align="center"><img src="/assets/img/blog/diagram5_ar_retrofit.svg" alt="AR-Retrofit side-channel between two frozen blocks" width="680"/></p> <p><em>Diagram 5. The AR-Retrofit side-channel sitting between two frozen blocks. The base path (amber spine) is the standard residual, still present. Off to the side, the router forms \(r_n\), the difference \(r_n - h_{n-1}\) is run through a low-rank adapter, multiplied by the scalar gate \(\gamma_n\), and added back to the stream. At \(\gamma_n = 0\) the side-channel is silent and the model reproduces the frozen backbone exactly.</em></p> <h2 id="training-the-retrofit">Training the retrofit</h2> <p>The frozen backbone is the teacher. The retrofit is trained with a three-term loss:</p> \[\mathcal{L} = \underbrace{\mathrm{CE}(x, y)}_{\text{full path}} \;+\; \lambda_{\text{kl}}\, \underbrace{\mathrm{KL}\!\big(p_{\text{teacher}} \,\|\, p_{\text{skip}}\big)}_{\text{makes blocks skip-friendly}} \;-\; \lambda_{\text{ent}}\, \underbrace{\mathbb{E}\big[H(\alpha)\big]}_{\text{prevents collapse}}\] <ul> <li><strong>Full-path CE.</strong> Ordinary next-token prediction, end-to-end through the retrofitted model with all blocks active.</li> <li><strong>Skip-branch KL.</strong> On each step, randomly mask one block (replace \(\mathrm{Block}_n\) with the identity for the rest of the forward) and distill from the unmodified frozen teacher. This is the term that makes blocks <em>individually skippable</em> later. Without it, \(\alpha\) learns a routing the model happens to like but cannot survive any block being absent.</li> <li><strong>Entropy bonus.</strong> A small reward on \(\mathbb{E}[H(\alpha)]\) keeps the softmax from prematurely collapsing onto a single source. Without it, \(\alpha\) converges to near one-hot at \(i = n-1\) — which trains fine but throws away the entire point of the mechanism.</li> </ul> <p>The teacher is the unmodified frozen backbone; no separate teacher model is needed.</p> <h2 id="why-not-the-simpler-thing">Why not the simpler thing?</h2> <p>Three nearby designs do not work, and the reasons are instructive.</p> <p><strong>Observer-only routing.</strong> Compute \(\alpha\) but do not let it affect the forward pass — just use it as a diagnostic. This gives you the routing signal “for free” but the routing weights end up uncorrelated with anything useful, because nothing in the loss makes them correspond to block importance. You cannot get a useful signal out of a module the rest of the network is ignoring.</p> <p><strong>Direct interpolation toward \(r_n\).</strong> Set \(x_n = (1 - \gamma) h_{n-1} + \gamma\, r_n\) instead of using a low-rank adapter on the difference. Frozen blocks degrade quickly as \(\gamma\) rises, because the soft mass over earlier \(h_i\) pushes block \(n\)’s input off the distribution it was pretrained on. The adapter on \((r_n - h_{n-1})\) exists exactly to express a <em>small</em> correction; direct interpolation can express arbitrarily large ones, and the gradient signal pushes it that way.</p> <p><strong>\(\gamma\)-free, always-on adapter.</strong> Replace \(\gamma_n \cdot A_n(r_n - h_{n-1})\) with just \(A_n(r_n - h_{n-1})\) — initialize \(A_n\) near zero and trust SGD. This loses the identity-at-init guarantee. Even with conservative init, the first many thousand steps inject non-trivial noise into frozen-block inputs, denting perplexity in ways the model struggles to recover from. The explicit curriculum on \(\gamma\) is what makes the bridge from “exactly the base model” to “fully retrofitted” smooth.</p> <p>The pattern across the three: the routing module needs to <em>do</em> something for its weights to mean something, but what it does must be a correction small enough that the frozen backbone tolerates it. The recipe — gated, low-rank, on the difference — is the smallest configuration that satisfies both.</p> <h2 id="reskip-using-alpha-at-inference">ReSkip: using \(\alpha\) at inference</h2> <p>Now we have a model where \(\alpha_{i \to n}\) is a real, trained, pre-execution signal about how block \(n\) is using its predecessors. Two diagnostics make it usable as a skip signal.</p> <p><strong>Routing importance</strong> of block \(n\):</p> \[I(n) = \max_{l &gt; n} \mathbb{E}\!\left[\alpha_{n \to l}\right]\] <p>This is the largest expected attention any <em>future</em> block places on \(h_n\). If \(I(n)\) is small, no downstream block cares much about \(h_n\) on average.</p> <p><strong>Ablation cost</strong> of block \(n\):</p> \[A(n) = \frac{\mathrm{PPL}(\text{block } n \text{ removed})}{\mathrm{PPL}(\text{full})}\] <p>A direct measurement: how much worse does the model get if we just drop block \(n\)? Computed once on a calibration set.</p> <p>Neither alone is enough. \(I(n)\) low does not guarantee block \(n\) is safe to drop — a low-routing block can still be doing load-bearing work that happens to flow through the standard residual path. \(A(n)\) low does guarantee safety, but it is a property of the calibration set; it does not tell us <em>when</em> a particular input is a good time to skip.</p> <p>So we use them together. Offline, we compute \(A(n)\) for every \(n\) and form the <strong>eligible set</strong> \(P = \{n : A(n) &lt; 1 + \delta\}\) — blocks that ablation tells us are safe to drop. Online, for each input, we look at the <em>recent</em> routing weight onto \(h_{n-1}\):</p> \[w_{\text{recent}}(n) = \mathbb{E}_{\text{recent tokens}}\!\left[\alpha_{n-1 \to n}\right]\] <p>and skip block \(n\) when</p> \[w_{\text{recent}}(n) &gt; \tau_n \quad\text{AND}\quad n \in P \quad\text{AND}\quad |\text{skipped so far}| &lt; M_{\max}\] <p>where \(\tau_n\) is a calibrated quantile threshold per block, and \(M_{\max}\) caps how many blocks we are willing to drop. The first condition is the per-input decision; the second is the offline safety net; the third is a guardrail against pathological inputs that would tempt the policy into skipping everything.</p> <p>The condition \(w_{\text{recent}}(n) &gt; \tau_n\) is intentionally counterintuitive. A <em>high</em> routing weight onto the previous-state means block \(n\)’s input was mostly the standard residual — the routing module is not pulling new information from earlier states. That is exactly when block \(n\)’s contribution is most likely to be a small refinement that we can afford to skip.</p> <h3 id="a-margin-preservation-argument">A margin-preservation argument</h3> <p>A simple property explains why this works empirically. Let \(\Delta(x)\) be the top-1 minus top-2 score at the output under the full model, and \(\varepsilon(x)\) the maximum logit perturbation introduced by skipping eligible blocks. Then</p> \[2\varepsilon(x) &lt; \Delta(x) \;\Longrightarrow\; \text{top-1 prediction unchanged.}\] <p>In other words: as long as the perturbation introduced by skipping is smaller than half the margin, the prediction is identical. The eligible set \(P\) and the threshold \(\tau_n\) are calibrated to keep \(\varepsilon\) small; the entropy-regularized routing keeps \(\Delta\) healthy. The two together explain why ReSkip can drop multiple blocks per forward without measurable accuracy loss on most inputs.</p> <p align="center"><img src="/assets/img/blog/diagram6_wrecent_trace.png" alt="Online skip trigger trace" width="740"/></p> <p><em>Diagram 6. Online skip trigger for a single block over a sequence (synthetic, illustrative). The dashed line is the calibrated threshold \(\tau_n\). Tokens above the line trigger a skip (amber); tokens below are kept (dark). Easy spans — function words, common continuations — cluster above the threshold; hard spans — entity-heavy clauses, math — cluster below.</em></p> <h2 id="what-you-should-expect-to-see">What you should expect to see</h2> <p>If the recipe is doing its job, three things should hold up empirically:</p> <ul> <li><strong>Step-zero parity.</strong> At \(\gamma = 0\) the retrofitted model is the frozen backbone, output-for-output (up to numerical precision). This is a verifiable invariant, not an aspiration — if you do not see it, the adapter or normalization is wrong.</li> <li><strong>Modest gains over the base model.</strong> The routing module gets to redistribute mass over depth, which on most benchmarks buys a small but consistent improvement over the frozen model. Magnitudes will depend on the family, the scale, and the SFT mix — but small-positive is the regime to look for. Large gains on a single benchmark are usually a sign of overfit calibration, not a sign of an unusually good recipe.</li> <li><strong>Skippable budget under ReSkip.</strong> A trained \(\alpha\) should give you several blocks-per-forward of skip budget at near-base accuracy. If ReSkip is <em>not</em> free or near-free in expectation, either the eligible set \(P\) is too generous or the entropy regularization was too weak and \(\alpha\) collapsed.</li> </ul> <p>Parameter cost is the easy part: at adapter rank 256, the side-channel is well under half a percent of a multi-billion-parameter backbone. The router and bottleneck adapter are tiny compared to the FFN they sit next to.</p> <h2 id="things-to-watch-for">Things to watch for</h2> <p>A few failure modes that this design <em>does not</em> automatically protect against. Worth knowing in advance.</p> <p><strong>Family transfer is not free.</strong> The recipe was developed against a particular pretrained family. The reasoning behind each choice (identity-at-init via \(\gamma\) curriculum, low-rank correction on the difference, etc.) is general, but the empirical claim that block-level granularity at \(L = 4\) is the right plateau leans on the specific block structure of the backbone. Different attention/FFN aspect ratios may want different groupings.</p> <p><strong>SFT mix is load-bearing.</strong> A router that is supposed to learn to route gets a much weaker training signal from a narrow SFT mix. Mixes that are too narrow or too task-specific tend to push \(\alpha\) toward near-one-hot routing — the loss is fine, but the routing signal becomes vestigial and you have effectively rebuilt the standard residual at three times the cost. Diversity in the SFT data is what keeps the router alive.</p> <p><strong>Block-level, not per-token, routing.</strong> The router decides per-block, not per-token. This bounds how fine-grained the adaptive computation can be and is qualitatively different from MoD-style per-token approaches. Whether that matters depends on your workload; for tasks dominated by uniform-difficulty tokens it does not, for heavily mixed-difficulty workloads it can.</p> <p><strong>ReSkip thresholds are deployment-specific.</strong> Calibrating \(\tau_n\) and the eligible set \(P\) on text and then deploying onto, say, a VLA action stream is the kind of thing that looks like it should work and does not. Per-modality recalibration is a real cost; expect to budget for it.</p> <p><strong>Wall-clock is not the same as parameter cost.</strong> The router and adapter introduce small sequential dependencies between the routing computation and the block forward. On accelerators with aggressive kernel fusion, this can eat into the theoretical inference savings from ReSkip. Measure on your target hardware, not just on the box where you trained.</p> <h2 id="closing-the-loop">Closing the loop</h2> <p>We started Part 1 wondering what was hiding in the residual <code class="language-plaintext highlighter-rouge">+</code>. The answer, three posts later, is: a fixed-weight, input-independent identity that can be replaced with a learned, input-dependent softmax which gives us a usable pre-execution routing signal. That signal is useful for adaptive computation, useful for interpretability (the per-block \(w_l\) is a real artifact you can read), and useful for inference-time block skipping that pays back the cost of the routing module itself.</p> <p>The retrofit story is the practical version of that idea: you do not need to retrain a 4B-parameter backbone to get most of the benefit. Under half a percent new parameters, a gated low-rank correction on the difference from the standard residual, and a curriculum that bridges identity-at-init to a fully active router. Plus a skip policy that uses the routing weights to recover the cost.</p> <p>The most interesting open question is whether the routing weights are <em>interpretable</em> in a stronger sense — whether \(w_l\) across a trained model encodes anything like a learned computational graph over depth. That is a longer story than this series. For now: the <code class="language-plaintext highlighter-rouge">+</code> is a stand-in, and there are useful things to put in its place.</p>]]></content><author><name></name></author><category term="ml"/><category term="transformers"/><category term="attention"/><category term="adaptive-computation"/><category term="peft"/><summary type="html"><![CDATA[Part 3 — retrofitting attention residuals into a pretrained decoder, and using the routing weights to skip blocks at inference.]]></summary></entry><entry><title type="html">Attention, the Residual Stream, and What’s Hiding in the “+”</title><link href="https://wusimo.github.io/blog/2026/attention-and-residuals/" rel="alternate" type="text/html" title="Attention, the Residual Stream, and What’s Hiding in the “+”"/><published>2026-05-17T04:00:00+00:00</published><updated>2026-05-17T04:00:00+00:00</updated><id>https://wusimo.github.io/blog/2026/attention-and-residuals</id><content type="html" xml:base="https://wusimo.github.io/blog/2026/attention-and-residuals/"><![CDATA[<p><em>Part 1 of a series on attention residuals and AR-Retrofit.</em></p> <h2 id="why-this-series">Why this series</h2> <p>The <a href="https://nlp.seas.harvard.edu/annotated-transformer/">Annotated Transformer</a> derives attention beautifully. But it treats <code class="language-plaintext highlighter-rouge">x + Sublayer(LayerNorm(x))</code> as a given — a single line of code. That <code class="language-plaintext highlighter-rouge">+</code> is doing more work than it looks, and it is also the place where a recent line of research opens up interesting possibilities.</p> <p>This series builds up to an idea — call it <strong>AR-Retrofit</strong> — for converting a pretrained decoder into an adaptive-depth model without retraining the backbone. To explain it cleanly we need three pieces:</p> <ol> <li><em>(this post)</em> Attention, compactly, and what the residual is actually doing.</li> <li><em>(next)</em> <strong>Attention Residuals</strong>: making the depth combination learned and input-dependent.</li> <li><em>(after that)</em> <strong>AR-Retrofit and ReSkip</strong>: retrofitting attention residuals into a frozen pretrained model, and using the resulting routing signal to skip blocks at inference time.</li> </ol> <p>We are not going to re-derive attention from scratch — the Annotated Transformer already does that, and you should read it first if you have not. We will move quickly through the parts that matter for the rest of the series, and slow down on the residual.</p> <h2 id="attention-in-one-page">Attention in one page</h2> <p>Given a sequence of tokens with hidden states \(X \in \mathbb{R}^{n \times d}\), attention produces a new sequence by letting each position pull information from the others. The mechanism is three steps:</p> <ol> <li>Project each row to a <strong>query</strong>, <strong>key</strong>, and <strong>value</strong>:</li> </ol> \[Q = X W_Q, \quad K = X W_K, \quad V = X W_V\] <ol> <li>Compute attention weights as scaled dot-products between queries and keys, normalized with softmax:</li> </ol> \[A = \mathrm{softmax}\!\left(\frac{Q K^\top}{\sqrt{d_k}}\right)\] <ol> <li>Take a weighted average of values:</li> </ol> \[\mathrm{Attn}(Q, K, V) = A V\] <p>The \(\sqrt{d_k}\) keeps the dot-product variance from blowing up at large \(d_k\), where the softmax would otherwise collapse onto a single key.</p> <p><strong>Multi-head attention</strong> runs \(h\) copies of this in parallel on different projections, concatenates them, and applies an output projection:</p> \[\mathrm{MHA}(X) = \mathrm{Concat}(\mathrm{head}_1, \ldots, \mathrm{head}_h)\, W_O\] <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>def attention(Q, K, V):
    scores  = Q @ K.T / sqrt(d_k)
    weights = softmax(scores, dim=-1)
    return weights @ V

def multihead(X):
    heads = [attention(X @ Wq[i], X @ Wk[i], X @ Wv[i]) for i in range(h)]
    return concat(heads) @ Wo
</code></pre></div></div> <p>That is the <em>across-tokens</em> mixing operation. In a decoder the score matrix \(A\) is masked so position \(t\) can only attend to positions \(\le t\).</p> <h2 id="the-transformer-block">The transformer block</h2> <p>A transformer block alternates between attention (token mixing) and a feed-forward network (channel mixing):</p> \[\begin{aligned} y &amp;= x + \mathrm{MHA}(\mathrm{LN}(x)) \\ z &amp;= y + \mathrm{FFN}(\mathrm{LN}(y)) \end{aligned}\] <p>with \(\mathrm{FFN}(u) = W_2\,\sigma(W_1 u)\). This is the <strong>pre-norm</strong> variant; modern decoders almost universally use pre-norm because it trains more stably at depth.</p> <p>Two things are happening in each sub-layer:</p> <ul> <li><strong>LayerNorm</strong> rescales the input before the sublayer.</li> <li><strong>The residual <code class="language-plaintext highlighter-rouge">+</code></strong> adds the sublayer’s output back to its input.</li> </ul> <p>The first is a normalization and we will not say more about it. The second is the focus of this post.</p> <p align="center"><img src="/assets/img/blog/diagram1_transformer_block.svg" alt="A single pre-norm transformer block" width="420"/></p> <p><em>Diagram 1. A single transformer block. The hidden state \(x\) flows top-to-bottom. Two side-cars branch off — first MHA, then FFN — each preceded by LayerNorm and rejoined to the main path with a <code class="language-plaintext highlighter-rouge">+</code>. The amber spine is the straight-through identity path that the two <code class="language-plaintext highlighter-rouge">+</code> lines preserve.</em></p> <h2 id="three-views-of-the-residual">Three views of the residual</h2> <p>The recipe \(x_l = x_{l-1} + f_l(\mathrm{LN}(x_{l-1}))\) — add the sublayer’s output to its input with weight 1 — is doing three things at once. Any one of them would justify the design; together they are why every modern transformer has a residual stream.</p> <h3 id="1-gradient-flow">1. Gradient flow</h3> <p>Without residuals, a deep stack compounds Jacobians:</p> \[\frac{\partial x_L}{\partial x_0} = \prod_{l=1}^{L} \frac{\partial f_l}{\partial x_{l-1}}\] <p>If those Jacobians have spectral norm \(&lt; 1\), the gradient vanishes; if \(&gt; 1\), it explodes. With residuals,</p> \[\frac{\partial x_l}{\partial x_{l-1}} = I + \frac{\partial f_l}{\partial x_{l-1}}\] <p>The identity term keeps the gradient from vanishing — there is always a path from the loss back to early layers that does not pass through any nonlinear transformation. This is the original ResNet motivation, and it is what lets transformers stack to dozens or hundreds of blocks without training pathology.</p> <h3 id="2-identity-at-initialization">2. Identity at initialization</h3> <p>If \(f_l\) is initialized so that \(\mathbb{E}\!\left[\|f_l(x)\|\right] \ll \|x\|\) — which is what reasonable init schemes give you — then at step zero the block is approximately the identity:</p> \[x_l \approx x_{l-1}\] <p>Training is then learning a <em>deviation</em> from identity:</p> \[x_l = x_{l-1} + (\text{small correction the block has learned to add})\] <p>This is iterative refinement at the architectural level. Each block does not transform \(x\) into a fundamentally different thing; it nudges \(x\) in a direction that turns out to be useful for the loss.</p> <h3 id="3-the-residual-stream-as-working-memory">3. The residual stream as working memory</h3> <p>This is the view that matters most for the rest of the series. The sequence</p> \[x_0,\; x_1,\; x_2,\; \ldots,\; x_L\] <p>is not a sequence of progressively more abstract feature maps. It is a <strong>single \(d\)-dimensional buffer</strong> that every block reads from and writes to.</p> <p>Each block:</p> <ul> <li><strong>reads</strong> from \(x_{l-1}\) via LayerNorm and the sublayer’s input projections,</li> <li><strong>writes</strong> to \(x_l\) by adding its output back,</li> <li>and <strong>does not overwrite</strong> what was already there — earlier content is preserved by the identity path.</li> </ul> <p>This perspective comes from mechanistic-interpretability work (Elhage et al., 2021, <a href="https://transformer-circuits.pub/2021/framework/"><em>A Mathematical Framework for Transformer Circuits</em></a>), which calls \(x_l\) the <strong>residual stream</strong>. Different blocks learn to write information into different subspaces of the stream; later blocks learn to read those subspaces. The stream is a shared communication channel, not a pipeline of progressively transformed features.</p> <p>Once you see it this way, the residual <code class="language-plaintext highlighter-rouge">+</code> stops looking like a stability hack and starts looking like the architectural commitment that makes the rest of the system work.</p> <p align="center"><img src="/assets/img/blog/diagram2_residual_stream.svg" alt="Residual stream as a shared vertical buffer" width="440"/></p> <p><em>Diagram 2. The residual stream as a single \(d\)-dimensional buffer running top-to-bottom. Blocks 1..L are side-cars: each reads from the stream, computes, and writes its output back with a <code class="language-plaintext highlighter-rouge">+</code>. Earlier content is never overwritten.</em></p> <h2 id="what-is-hiding-in-the-">What is hiding in the “+”</h2> <p>The residual stream view makes one thing very obvious: every block’s output is added to the stream <strong>with a fixed weight of 1</strong>. Every block, every layer, every input — same scalar. The combination rule is</p> \[x_l = 1 \cdot x_{l-1} \;+\; 1 \cdot f_l(\mathrm{LN}(x_{l-1})).\] <p>Why 1? Because that is the identity element. It is the obvious choice when you do not have anything better.</p> <p>But once we view \(x_l\) as a write into a shared stream, two questions get hard to ignore:</p> <ul> <li><strong>Why should block \(l\) only see the <em>previous</em> state \(x_{l-1}\)?</strong> It could read from \(x_0, x_1, \ldots, x_{l-1}\) — every state is already computed and sitting in memory. The information is right there.</li> <li><strong>Why should the combination weights be a fixed scalar 1?</strong> Some inputs might benefit from drawing heavily on what block 3 wrote and ignoring block 7; another input might want the opposite.</li> </ul> <p>This is exactly the thing attention was invented for in a different context — letting a query selectively combine multiple sources. So:</p> <blockquote> <p>What if we used attention not across <em>tokens</em>, but across <em>depth</em>?</p> </blockquote> <p>That is the move in the next post.</p> <h2 id="looking-ahead">Looking ahead</h2> <p>In Part 2 we will see how <strong>Attention Residuals</strong> (Kimi Team, 2026) replace the fixed <code class="language-plaintext highlighter-rouge">+</code> with a learned attention over previous block outputs. Each block computes a softmax over \(\{x_0, x_1, \ldots, x_{l-1}\}\) and reads a routed combination. This buys two things:</p> <ol> <li>The depth combination becomes <strong>input-dependent</strong> — different inputs traverse different effective paths through the stack.</li> <li>The routing signal is <strong>available before the block runs</strong>, which turns out to be useful for adaptive computation.</li> </ol> <p>In Part 3 we get to the practical problem: existing pretrained models — Qwen, Llama, the VLM backbones we actually want to deploy — were trained with the fixed <code class="language-plaintext highlighter-rouge">+</code>. Can we retrofit attention residuals into them without retraining the backbone? That is the question AR-Retrofit tries to answer, and the design that falls out of taking it seriously is the topic of the last post.</p>]]></content><author><name></name></author><category term="ml"/><category term="transformers"/><category term="attention"/><category term="residuals"/><summary type="html"><![CDATA[Part 1 — what the residual `+` in a transformer is actually doing, and why we should care.]]></summary></entry><entry><title type="html">Attention Residuals: Making the “+” Learned</title><link href="https://wusimo.github.io/blog/2026/attention-residuals/" rel="alternate" type="text/html" title="Attention Residuals: Making the “+” Learned"/><published>2026-05-17T04:00:00+00:00</published><updated>2026-05-17T04:00:00+00:00</updated><id>https://wusimo.github.io/blog/2026/attention-residuals</id><content type="html" xml:base="https://wusimo.github.io/blog/2026/attention-residuals/"><![CDATA[<p><em>Part 2 of a series on attention residuals and AR-Retrofit. <a href="/blog/2026/attention-and-residuals/">Part 1</a> set up the residual stream and the question this post answers.</em></p> <h2 id="picking-up-from-post-1">Picking up from Post 1</h2> <p>We ended the last post with two questions about the residual <code class="language-plaintext highlighter-rouge">+</code>:</p> <ul> <li>Why should block \(l\) only see the immediately previous state \(x_{l-1}\)? Every earlier state \(x_0, x_1, \ldots, x_{l-1}\) is already computed and sitting in the residual stream.</li> <li>Why should the combination weight be a fixed scalar 1, the same for every block, every layer, every input?</li> </ul> <p>The natural reaction once you see those questions is: <em>attention</em>. The whole point of attention is to let a query selectively combine multiple sources. We already use it across tokens. Why not across depth?</p> <p>That is the move in <strong>Attention Residuals</strong> (Kimi Team, 2026). This post walks through what changes when you make it.</p> <h2 id="attention-residuals-in-one-page">Attention residuals in one page</h2> <p>Recall the standard residual recipe:</p> \[x_l = x_{l-1} + f_l(\mathrm{LN}(x_{l-1}))\] <p>where \(f_l\) is the \(l\)-th block (MHA + FFN, in pre-norm form). The <code class="language-plaintext highlighter-rouge">+</code> is a fixed scalar-1 combination of two things: the previous state \(x_{l-1}\), and the block’s contribution \(f_l(\cdot)\).</p> <p>In <strong>Attention Residuals (AttnRes)</strong>, that combination becomes a learned softmax-weighted sum over <em>all</em> previous block outputs.</p> <p>Let \(h_i\) denote the output of block \(i\) — what would have been added to the residual stream in the standard recipe. AttnRes replaces the <code class="language-plaintext highlighter-rouge">+</code> with:</p> \[\begin{aligned} k_i &amp;= W_K\, h_i \\ \alpha_{i \to l} &amp;= \mathrm{softmax}_i\!\left(\frac{w_l^\top k_i}{\sqrt d}\right), \quad i = 0, 1, \ldots, l-1 \\ x_l &amp;= \sum_{i=0}^{l-1} \alpha_{i \to l}\, h_i \end{aligned}\] <p>Three new objects show up:</p> <ul> <li>\(w_l \in \mathbb{R}^d\) is a <strong>learned pseudo-query</strong> owned by block \(l\). There is one per block; no per-token query.</li> <li>\(k_i = W_K h_i\) are <strong>keys</strong> projected from each prior block output. \(W_K\) is shared across blocks.</li> <li>\(\alpha_{i \to l}\) is a softmax over depth — for each layer \(l\), a distribution over the \(l\) prior states.</li> </ul> <p>The result \(x_l\) is the input to block \(l\). The block itself (\(f_l\)) is unchanged: it is still MHA + FFN, still operating on a \(d\)-dimensional input. What changed is <em>how that input is assembled from the history of the stream</em>.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>def attention_residual(history, l):
    # history = [h_0, h_1, ..., h_{l-1}]
    K = stack([W_K @ h for h in history])      # (l, d)
    scores  = (w[l] @ K.T) / sqrt(d)           # (l,)
    alpha   = softmax(scores)                  # (l,)
    x_l     = sum(alpha[i] * history[i] for i in range(l))
    return x_l
</code></pre></div></div> <p>That is the entire mechanism. No per-token routing; no extra normalization layers; one query vector per block, one shared key projection.</p> <p align="center"><img src="/assets/img/blog/diagram3_attnres_fanin.svg" alt="Attention Residual fan-in" width="620"/></p> <p><em>Diagram 3. Block \(l\)’s input is no longer a single <code class="language-plaintext highlighter-rouge">+</code> from \(x_{l-1}\) — it is a softmax-weighted fan-in from every prior output \(h_0, \ldots, h_{l-1}\). The router (dashed) computes \(\alpha\) before block \(l\) runs; edge thickness illustrates the weight magnitude.</em></p> <h2 id="recovering-the-standard-residual">Recovering the standard residual</h2> <p>A useful sanity check: AttnRes is a strict generalization of the standard <code class="language-plaintext highlighter-rouge">+</code>. If we constrain \(\alpha\) to put all its mass on \(i = l-1\), we recover</p> \[x_l = h_{l-1} = x_{l-1} + f_{l-1}(\mathrm{LN}(x_{l-1}))\] <p>which is the standard recipe (with \(h_{l-1}\) playing the role of the post-residual state, depending on whether you define \(h\) as block-output-only or full-state — Kimi’s paper uses the latter). Initializing \(w_l\) to peak on \(i = l-1\), or warming up from a one-hot at \(i=l-1\) toward learned weights, gives an identity-at-initialization that mirrors how standard residuals already start. This will matter a lot in Post 3.</p> <h2 id="three-views-of-attention-residuals">Three views of attention residuals</h2> <p>The fixed <code class="language-plaintext highlighter-rouge">+</code> of Post 1 was doing three things at once: enabling gradient flow, providing identity-at-init, and acting as a working-memory write into a shared stream. The learned version inherits all three — and then adds three more capabilities of its own.</p> <h3 id="1-input-dependent-depth-combination">1. Input-dependent depth combination</h3> <p>In the standard residual, every input traverses the same path: every block contributes with the same weight 1. The depth of computation is fixed by the architecture.</p> <p>With AttnRes, different inputs induce different distributions over \(\alpha\). An easy token (say, completing a common bigram) might end up with \(\alpha\) concentrated on shallow \(i\) — the model is signaling that it does not need deep blocks. A hard token (say, multi-hop reasoning) might spread \(\alpha\) across deep blocks, or weight a specific mid-stack block heavily.</p> <p>This is <em>adaptive computation</em>, but not in the explicit halting sense of <a href="https://arxiv.org/abs/1603.08983">ACT</a> or the per-token routing of <a href="https://arxiv.org/abs/2404.02258">MoD</a>. It is a softer thing: the <em>combination</em> over depth is input-dependent, even though every block still runs. The model decides which prior states matter for the current block, not whether to skip.</p> <h3 id="2-a-routing-signal-that-exists-before-the-block-runs">2. A routing signal that exists <em>before</em> the block runs</h3> <p>This is the property that turns out to be most useful, and it is easy to miss.</p> <p>Look again at the order of operations:</p> <ol> <li>Compute \(\alpha_{i \to l}\) from prior outputs \(h_0, \ldots, h_{l-1}\) and the learned \(w_l\).</li> <li>Form \(x_l = \sum_i \alpha_{i \to l} h_i\).</li> <li>Run block \(l\) on \(x_l\) to produce \(h_l\).</li> </ol> <p>Step 1 finishes before step 3 starts. The weights \(\alpha_{\cdot \to l}\) are a <em>pre-execution</em> signal about how much block \(l\)’s input is drawing on each predecessor — and, by symmetry, how much <em>future</em> blocks will draw on \(h_l\) once it exists.</p> <p>This is the foothold that ReSkip (Post 3) uses to decide which blocks to skip at inference. If we can read off “block \(l\) is barely contributing to the stream” before block \(l\) runs, we can skip it. Standard residuals give us no such signal: every block contributes with weight 1 by definition.</p> <h3 id="3-depth-as-a-soft-mixture-not-a-strict-ordering">3. Depth as a soft mixture, not a strict ordering</h3> <p>The standard residual stream is sequential by construction: block \(l\) reads only what block \(l-1\) wrote (plus everything that survived the identity path). AttnRes lets block \(l\) read directly from \(h_3\) or \(h_7\) without going through \(h_{l-1}\).</p> <p>This breaks the implicit assumption that information has to <em>survive</em> intermediate writes to reach a later block. In standard transformers, a feature written by block 3 only reaches block 15 if blocks 4 through 14 do not stomp on it. AttnRes lets block 15 reach back to \(h_3\) directly. The residual stream stops being a strictly ordered tape and becomes more like a content-addressable memory indexed by depth.</p> <p>The Kimi paper finds that this matters most at moderate depths and with block-level (not layer-level) granularity — grouping \(S \in \{2, 4, 8\}\) layers into a block before applying AttnRes. Layer-level granularity is too fine; the router has nothing useful to say between adjacent layers.</p> <h2 id="what-is-hiding-in-w_l">What is hiding in \(w_l\)</h2> <p>The learned pseudo-query \(w_l\) is the new degree of freedom, and it deserves a beat.</p> <p>In standard attention, queries are per-token: each position computes its own query from its hidden state. In AttnRes, the query is per-block: it is a single vector, learned once, that does not depend on the input. The keys are input-dependent (they come from \(h_i\), which depends on the input), but the query is not.</p> <p>That seems like a strong restriction, and it is — but it is also what makes the mechanism cheap and what makes the routing weights <em>interpretable as a property of the block</em>, not of the token. “Block \(l\) tends to read from blocks 3, 7, and \(l-1\)” is a statement about block \(l\). If \(w_l\) were per-token, we would have to talk about the routing of each (block, token) pair separately, and the signal would be much noisier.</p> <p>This is also why AttnRes is comparatively cheap. The added parameters per block are \(w_l \in \mathbb{R}^d\) and a shared \(W_K \in \mathbb{R}^{d \times d}\) — that is one vector per block plus one matrix total, against the millions of parameters in the block itself.</p> <p align="center"><img src="/assets/img/blog/diagram4_alpha_heatmap.png" alt="Heatmap of routing weights across depth" width="560"/></p> <p><em>Diagram 4. A heatmap of \(\alpha_{i \to l}\) across depth (synthetic, illustrative). Rows are queries (block \(l\)), columns are predecessors \(i\). The standard residual would be a strict subdiagonal one-hot; AttnRes shows a band along the subdiagonal plus a few off-diagonal hotspots where late blocks reach back to early ones.</em></p> <h2 id="looking-ahead">Looking ahead</h2> <p>AttnRes gives us two new things compared to the standard <code class="language-plaintext highlighter-rouge">+</code>:</p> <ol> <li>The depth combination is <strong>input-dependent</strong> and <strong>learned</strong>.</li> <li>The routing weights \(\alpha\) are <strong>available before each block runs</strong>, which makes them usable as a control signal.</li> </ol> <p>There is one large catch: the recipe above assumes we are training from scratch. Every existing pretrained decoder — Qwen, Llama, the VLM backbones we actually deploy — was trained with the fixed <code class="language-plaintext highlighter-rouge">+</code>. Their weights are entangled with the assumption that block \(l\) reads from \(x_{l-1}\), and nothing else.</p> <p>The question for Part 3 is the practical one: <strong>can we retrofit attention residuals into a frozen pretrained model?</strong> Without retraining the backbone, ideally with under half a percent of new parameters, and ideally in a way that lets us use the routing signal to skip blocks at inference. That is AR-Retrofit and ReSkip, and the answer is yes — with some design choices that turn out to matter.</p>]]></content><author><name></name></author><category term="ml"/><category term="transformers"/><category term="attention"/><category term="adaptive-computation"/><summary type="html"><![CDATA[Part 2 — replacing the fixed `+` with a softmax over prior block outputs.]]></summary></entry><entry><title type="html">Some Nonsense About Bitcoin</title><link href="https://wusimo.github.io/blog/2018/some-nonsense-about-bitcoin/" rel="alternate" type="text/html" title="Some Nonsense About Bitcoin"/><published>2018-01-24T20:08:59+00:00</published><updated>2018-01-24T20:08:59+00:00</updated><id>https://wusimo.github.io/blog/2018/some-nonsense-about-bitcoin</id><content type="html" xml:base="https://wusimo.github.io/blog/2018/some-nonsense-about-bitcoin/"><![CDATA[<h1 id="background">Background</h1> <p>During one boring afternoon, one of my friend in Boston sent me an interesting plot:</p> <p><a href="https://ibb.co/mcVxWw" class="img-responsive"><img src="/assets/img/blog/BTC.jpg" alt="BTC Price vs BTC Mining Difficulty"/></a></p> <p>Certainly the correlation between bitcoin price and bitcoin mining difficulty is obvious. But the question imediately pops up in my mind: Why? What is the cause behind this strong correlation?</p> <p>Let’s start with this simple question: What are those people who devoted into bitcoin mining from the earliest days like?</p> <p>The answer is simple: they believe in bitcoin and want to drive up the bitocin price.</p> <p>Based on this simple thought, I come up with a simple model, which models the interaction between bitocoin price, bitcoin mining difficulty, total bitcoin numbers and total bitcoin mining power.</p> <h1 id="model">Model</h1> <p>I have to admit that this is a little bit math heavy for some of the users so if you are bored by the math, please just jump the modeling part. For the math expert people, I have to admit that I haven’t done rigorous proofs and everything is questionable here.</p> <p>I have used stochastic optimal control to model the evolution of bitcoin price and the actions taken by the main bitcoin miners. And eventually use this to show this will leads to the correlation between the mining difficulty and bitcoin price over the past five years which we have discovered in the history data.</p> <p>The basic assumption of this model is as follows:</p> <ol> <li> <p>The state space we are interested in is in \(R^4\).</p> \[Z(t) = (P(t),D(t),N(t),C(t))\] <p>where \(P(t)\) is the ‘bitcoin price’ at time t, \(D(t)\) is the ‘mining difficulty’ at time t, \(N(t)\) is the ‘total number of coins already been mined’, and $C(t)$ is the current ‘total mining power’ measured in unit TH.</p> </li> <li> <p>For simplicity we only consider only one main bitcoin miner and that big player can make two kinds of constrained decision at each time: whether to invest more money on mining or to decrease the current mining power. So abstractly the dynamic of the state can be descrbed as:</p> \[\frac{d}{dt}Z(t) = F(Z(t),\nu(t)) , t&gt;0,\] \[Z(0) = (P_0,D_0,N_0,C_0)\] <p>where \(\nu(t)\) is the control term. \(P_0\) is the initial bitcoin price, \(D_0\) is the initial bitcoin mining difficulty, etc…</p> <p>As we mentioned earlier the ‘Big Player’ wants to maximize his profit in long term because he believes in Bitcoin and also wants to minimize his total cost spent on mining. We use the value functional J to represent his earning.</p> </li> <li> <p>The value functional J is the functional which we want to maximize. Our main assumption here is that the big miner will always hold the mined bitcoin and eventually want to maximize their value with limited control. To write out:</p> \[\nu(t) = sup_{\nu\in A} N(T)\cdot P(t)-\int_{0}^{T} \nu(t) dt\] <p>where A is the admissible control set.</p> </li> </ol> <p>Under this abstract settings, we need to develop a reasonable function F which will somehow recover the dynamic of the whole system.</p> \[\frac{d}{dt}P(t) = P(t)(\mu dt+\sigma_1 dW)-f_{1}(\nu(t))dt\] <p>where \(f_{1}\) is a stricty increasing positive function, since adding more mining power will increase the provide of bitcoins and will drive down the price in short time.</p> \[\frac{d}{dt}D(t) = f_{2}(N(t),C(t))dt+\sigma_2 dW\] <p>This has the meaning of the mining difficulty is adjusted by the current mining power and number of coins that has been already mined.</p> \[\frac{d}{dt}N(t) = C_1 C(t)/D(t) dt+\sigma_3 dW\] <p>Means: the number of the coins that already have been minded increases with the speed of current mining power divided by the current mining difficulty.</p> \[\frac{d}{dt}C(t) = (1-C_2)dt+\nu(t)\] <p>This means that the mining rig is going to be out of date with the ratio of $C_2$ and here is the control takes part, we can either increase the mining power which will drag down the price in short term and increase the difficulty or to decrease the mining power to have the opposite effect and hope to have a better payoff in long term.</p> <p>We can rewrite our model into a more concise and abstract form:</p> \[\frac{d}{dt}\begin{pmatrix}P(t)\\D(t)\\N(t)\\C(t)\end{pmatrix} = \mu(t,P(t),D(t),N(t),C(t),\nu(t))dt+\sigma dW\] <p>where \(\mu(t,P(t),D(t),N(t),C(t),\nu(t))= \begin{pmatrix} \mu_1 P(t)-f_{1}(\nu(t))\\f_{2}(N(t),C(t)) \\C_{1}C(t)/D(t)\\ 1-C_{2}+\nu(t)\end{pmatrix}\)</p> <p>dW is a vector of independent white noises.</p> <p>and \(\sigma = \begin{pmatrix}\sigma_{1}\\\sigma_{2} \\\sigma_{3} \\0 \end{pmatrix}\) is the volatilitly constant.</p> <p>In order to apply the Dynamic Programming Principle(DPP), we have to check that our model meets the 4 assumptions:</p> <p>Assumption 1: to be added…</p> <p>Assumption 2: The affect of the control $\nu$ is causal, as we can see any two same control will give us the same result in the sense of expectation.</p> <p>Assumption 3: Concatenating two admissible controls will also give us an admissible control by the construction of our admissible controls set.</p> <p>Assumption 4: It is easy and obvious to see that our value function J is additive. Now we are in good shape to apply the Ito’s formula to derive the Dynamic Programming Equation(DPE). In our finite horizon setting, the DPE equation reads:</p> \[-\frac{\partial}{\partial t} v(t,P,D,N,C)+H(P,D,N,C,t,\nabla v(t,P,D,N,C),D^{2}v(t,P,D,N,C)) = 0\] <p>where</p> <p>\(H()\) plug in our stochastic dynamic system and choosing simple form of functions $f_{1},f_{2}$,we have:</p> <h1 id="simulation">Simulation</h1> <p>I have runned 1000 Monte Carlo Simulations, the code are provided below:</p> <figure class="highlight"><pre><code class="language-ruby" data-lang="ruby"><span class="n">import</span> <span class="n">numpy</span> <span class="n">as</span> <span class="n">np</span>
<span class="n">import</span> <span class="n">math</span>
<span class="n">import</span> <span class="n">matplotlib</span><span class="p">.</span><span class="nf">pyplot</span> <span class="n">as</span> <span class="n">plt</span>
<span class="n">from</span> <span class="n">scipy</span><span class="p">.</span><span class="nf">stats</span> <span class="n">import</span> <span class="n">norm</span>

<span class="c1"># simply forward the stochastic ODE using explicit scheme</span>
<span class="no">NN</span> <span class="o">=</span> <span class="mi">500</span>
<span class="no">P</span> <span class="o">=</span> <span class="p">[</span><span class="mi">0</span><span class="p">]</span><span class="o">*</span> <span class="no">NN</span>
<span class="no">D</span> <span class="o">=</span> <span class="p">[</span><span class="mi">0</span><span class="p">]</span><span class="o">*</span> <span class="no">NN</span>
<span class="no">N</span> <span class="o">=</span> <span class="p">[</span><span class="mi">0</span><span class="p">]</span><span class="o">*</span> <span class="no">NN</span>
<span class="no">C</span> <span class="o">=</span> <span class="p">[</span><span class="mi">0</span><span class="p">]</span><span class="o">*</span> <span class="no">NN</span>
<span class="n">mu</span><span class="o">=</span> <span class="p">[</span><span class="mi">0</span><span class="p">]</span><span class="o">*</span> <span class="no">NN</span>
<span class="n">dt</span> <span class="o">=</span> <span class="mf">0.01</span>
<span class="n">mu_1</span> <span class="o">=</span> <span class="mi">1</span>
<span class="n">sigma_1</span> <span class="o">=</span> <span class="mf">0.1</span>
<span class="no">C1</span> <span class="o">=</span> <span class="mi">5</span>
<span class="no">C2</span> <span class="o">=</span> <span class="mf">0.99</span>

<span class="c1"># initial conditon</span>
<span class="no">P</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">=</span> <span class="mi">100</span>
<span class="no">D</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">=</span> <span class="mi">100</span>
<span class="no">N</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">=</span> <span class="mi">100</span>
<span class="no">C</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">=</span> <span class="mi">100</span>

<span class="k">def</span> <span class="nf">f1</span><span class="p">(</span><span class="n">x</span><span class="p">):</span>
    <span class="k">return</span> <span class="n">x</span><span class="o">*</span><span class="n">x</span><span class="o">*</span><span class="n">x</span>

<span class="k">def</span> <span class="nf">f1primeinverse</span><span class="p">(</span><span class="n">x</span><span class="p">):</span>
    <span class="k">return</span> <span class="mi">1</span>

<span class="k">def</span> <span class="nf">f2</span><span class="p">(</span><span class="n">x</span><span class="p">,</span><span class="n">y</span><span class="p">):</span>
    <span class="k">return</span> <span class="n">x</span><span class="o">*</span><span class="n">y</span>

<span class="k">for</span> <span class="n">j</span> <span class="k">in</span> <span class="n">range</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span><span class="mi">100</span><span class="p">):</span>
    <span class="k">for</span> <span class="n">i</span> <span class="k">in</span> <span class="n">range</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span><span class="no">NN</span><span class="p">):</span>
        <span class="c1">#mu_i = np.exp(np.random.normal(0,1))</span>
        <span class="n">mu_i</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">((</span><span class="no">C</span><span class="p">[</span><span class="n">i</span><span class="o">-</span><span class="mi">1</span><span class="p">]</span><span class="o">*</span><span class="no">P</span><span class="p">[</span><span class="n">i</span><span class="o">-</span><span class="mi">1</span><span class="p">]</span><span class="o">/</span><span class="p">(</span><span class="no">D</span><span class="p">[</span><span class="n">i</span><span class="o">-</span><span class="mi">1</span><span class="p">]</span><span class="o">*</span><span class="no">D</span><span class="p">[</span><span class="n">i</span><span class="o">-</span><span class="mi">1</span><span class="p">]))</span><span class="o">+</span><span class="p">(</span><span class="mi">1</span><span class="o">/</span><span class="no">N</span><span class="p">[</span><span class="n">i</span><span class="o">-</span><span class="mi">1</span><span class="p">]))</span>
        <span class="n">dP</span> <span class="o">=</span> <span class="p">(</span><span class="n">mu_1</span><span class="o">*</span><span class="no">P</span><span class="p">[</span><span class="n">i</span><span class="o">-</span><span class="mi">1</span><span class="p">]</span><span class="o">-</span><span class="n">f1</span><span class="p">(</span><span class="n">mu_i</span><span class="p">))</span><span class="o">*</span><span class="n">dt</span><span class="o">+</span> <span class="no">P</span><span class="p">[</span><span class="n">i</span><span class="o">-</span><span class="mi">1</span><span class="p">]</span><span class="o">*</span><span class="n">sigma_1</span><span class="o">*</span><span class="n">np</span><span class="p">.</span><span class="nf">random</span><span class="p">.</span><span class="nf">normal</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span><span class="mi">1</span><span class="p">)</span>
        <span class="n">dD</span> <span class="o">=</span> <span class="n">f2</span><span class="p">(</span><span class="no">N</span><span class="p">[</span><span class="n">i</span><span class="o">-</span><span class="mi">1</span><span class="p">],</span><span class="no">C</span><span class="p">[</span><span class="n">i</span><span class="o">-</span><span class="mi">1</span><span class="p">])</span><span class="o">*</span><span class="n">dt</span>
        <span class="n">dN</span> <span class="o">=</span> <span class="p">(</span><span class="no">C1</span><span class="o">*</span><span class="no">C</span><span class="p">[</span><span class="n">i</span><span class="o">-</span><span class="mi">1</span><span class="p">]</span><span class="o">/</span><span class="no">D</span><span class="p">[</span><span class="n">i</span><span class="o">-</span><span class="mi">1</span><span class="p">])</span><span class="o">*</span><span class="n">dt</span> <span class="c1"># dN should be a constant according to the bitcoin settings</span>
        <span class="n">dC</span> <span class="o">=</span> <span class="no">C</span><span class="p">[</span><span class="n">i</span><span class="o">-</span><span class="mi">1</span><span class="p">]</span><span class="o">*</span><span class="p">(</span><span class="mi">1</span><span class="o">-</span><span class="no">C2</span><span class="o">+</span><span class="n">mu_i</span><span class="p">)</span><span class="o">*</span><span class="n">dt</span>
        <span class="c1">#print dP,dD,dN,dC</span>
        <span class="no">P</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">=</span> <span class="no">P</span><span class="p">[</span><span class="n">i</span><span class="o">-</span><span class="mi">1</span><span class="p">]</span><span class="o">+</span><span class="n">dP</span>
        <span class="no">D</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">=</span> <span class="no">D</span><span class="p">[</span><span class="n">i</span><span class="o">-</span><span class="mi">1</span><span class="p">]</span><span class="o">+</span><span class="n">dD</span>
        <span class="no">N</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">=</span> <span class="no">N</span><span class="p">[</span><span class="n">i</span><span class="o">-</span><span class="mi">1</span><span class="p">]</span><span class="o">+</span><span class="n">dN</span>
        <span class="no">C</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">=</span> <span class="no">C</span><span class="p">[</span><span class="n">i</span><span class="o">-</span><span class="mi">1</span><span class="p">]</span><span class="o">+</span><span class="n">dC</span>
    <span class="n">plt</span><span class="p">.</span><span class="nf">plot</span><span class="p">(</span><span class="no">P</span><span class="p">)</span>
<span class="n">plt</span><span class="p">.</span><span class="nf">show</span><span class="p">()</span></code></pre></figure> <h1 id="result">Result</h1> <h1 id="btc-price">BTC Price</h1> <p><a href="https://ibb.co/mcVxWw" class="img-responsive"><img src="/assets/img/blog/BTC2.png" alt="BTC Price "/></a></p> <h1 id="total-number-of-bitcoins">Total number of bitcoins</h1> <p><a href="https://ibb.co/mcVxWw" class="img-responsive"><img src="/assets/img/blog/BTC3.png" alt="Total number of bitcoins"/></a></p> <h1 id="btc-mining-difficulty">BTC Mining Difficulty</h1> <p><a href="https://ibb.co/mcVxWw" class="img-responsive"><img src="/assets/img/blog/BTC4.png" alt="BTC Mining Difficulty"/></a> The result has showed the time evolution of the probabilty density of the Bitcoin Price, Mining Difficulty, Total Number of Coins.</p> <p>We can see that this model has an upper bound of total number of coins which meets well with the design of Bitcoin.</p> <p>And the Price has certain correlation with mining difficulty and both tends to grow exponentially.</p> <h1 id="bitcoin-tipjar">BitCoin TipJar</h1> <p>BTC Address: 112ca6RbrargroNBvosVZgY7BzmTsm7hhT</p> <p><a href="https://ibb.co/mcVxWw" class="img-responsive"><img src="/assets/img/blog/BTC5.png" alt="BTC TipJar"/></a></p> <h1 id="ethereum-tipjar">Ethereum TipJar</h1> <p>ETH Address: 0x40B693679D7984aEb0062C25Ca3d406ECD2Aff41</p> <p><a href="https://ibb.co/mcVxWw" class="img-responsive"><img src="/assets/img/blog/ETH.png" alt="BTC TipJar"/></a></p>]]></content><author><name></name></author><category term="archive"/><category term="bitcoin"/><category term="economics"/><summary type="html"><![CDATA[Notes on the strong correlation between bitcoin price and mining difficulty.]]></summary></entry></feed>